hi everyone<br>
this is final project<br>
we are going to design a simplified traffic system<br>
## ⚠️ Team Rules (Must Follow)<br>

We use function-based interfaces through a single simulator.<br>
The ⁠ shared/ ⁠ folder serves as a strict contract between modules (v-group outputs ⁠ CarAction ⁠, i-group outputs ⁠ SignalAction ⁠) and MUST remain identical for all members.<br>
Do NOT modify any files in ⁠ shared/ ⁠ (e.g., ⁠ state.py ⁠, ⁠ enums.py ⁠, ⁠ actions.py ⁠, ⁠ topology.py ⁠).<br>
Without informing the entire team, explaining the change and its impact, and getting agreement first.<br>

Simulation rules: slot range is 0→29, crossing happens from slot 29 to intersection, red light must be checked before entering the intersection, cars must not move if another car is visible ahead, and at most one car is allowed per intersection per step<br>

Wrokflow for reference:<br>
git fetch origin<br>
git pull origin main<br>

git add .<br>
git push origin main<br>